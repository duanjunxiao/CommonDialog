<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_common_dialog"
    android:orientation="vertical"
    android:paddingLeft="20dp"
    android:paddingRight="20dp">

    <TextView
        android:id="@+id/title"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="15dp"
        android:layout_marginTop="25dp"
        android:gravity="center"
        android:textColor="#333"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="project_delete_title" />

    <TextView
        android:id="@+id/message"
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:gravity="start"
        android:lineSpacingMultiplier="1.3"
        android:textColor="#888"
        android:textSize="12sp"
        android:textStyle="bold"
        tools:text="project_delete_content" />

    <LinearLayout
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginBottom="20dp"
        android:layout_marginTop="20dp"
        android:gravity="start"
        android:orientation="horizontal">

        <Button
            android:id="@+id/negativeButton"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="110dp"
            android:layout_height="36dp"
            android:layout_marginRight="20dp"
            android:layout_weight="11"
            android:background="@drawable/common_white_corner_selector"
            android:text="@string/action_cancel"
            android:textColor="#888"
            android:textSize="12sp" />

        <Button
            android:id="@+id/positiveButton"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="110dp"
            android:layout_height="36dp"
            android:layout_gravity="end"
            android:layout_weight="11"
            android:background="@drawable/common_blue_corner_selector"
            android:text="@string/action_sure"
            android:textColor="@color/common_white"
            android:textSize="12sp" />
    </LinearLayout>

</LinearLayout>